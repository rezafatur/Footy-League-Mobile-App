import 'package:flutter/material.dart';

class PDStandingsWidget extends StatelessWidget {
  const PDStandingsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("PD");
  }
}
